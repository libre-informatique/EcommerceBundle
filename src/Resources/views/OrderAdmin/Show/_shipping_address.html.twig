{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field_value %}
    {% include '@SyliusAdmin/Common/_address.html.twig' with {'address': object.shippingAddress} %}
    <div>
        <a href="{{ path('sylius_admin_order_update', {'id': object.id}) }}" class="ui icon labeled tiny fluid button">
            <i class="pencil icon"></i> {{ 'sylius.ui.edit'|trans }}
        </a>
    </div>
{% endblock %}